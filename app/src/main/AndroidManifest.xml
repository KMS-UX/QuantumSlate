<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
        Location permissions are intentionally NOT declared. The Bible §6 lists them for
        weather, but this build never reads device location: the user types a place name in
        Settings and it is resolved with Geocoder, which needs no permission. Declaring an
        unused location permission would show users a prompt for a capability the app does
        not have. Re-add both if GPS-based weather is ever implemented.
    -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- READ only: the calendar repository never writes, so WRITE_CALENDAR is not declared. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".QuantumSlateApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuantumSlate"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.QuantumSlate"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Remove WorkManager's default on-startup initializer: the app supplies its own
            Configuration (with the Hilt worker factory) from QuantumSlateApplication.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Real-Time update mode (Bible §4); opt-in, with a battery warning on its notification -->
        <service
            android:name=".work.RealtimeSyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Spotify OAuth redirect target; scheme must match SpotifyAuth.REDIRECT_URI -->
        <activity
            android:name=".data.remote.spotify.SpotifyRedirectActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="quantumslate" android:host="spotify-callback" />
            </intent-filter>
        </activity>

        <!-- Time & Weather Home Screen Widget -->
        <receiver
            android:name=".widget.TimeWeatherWidget"
            android:exported="true"
            android:label="@string/widget_time_weather">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.quantumslate.dashboard.REFRESH_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/time_weather_widget_info" />
        </receiver>

    </application>

</manifest>
